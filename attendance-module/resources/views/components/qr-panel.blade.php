<div class="border rounded p-4 bg-white shadow">
    <h2 class="text-lg font-semibold">{{ $title }}</h2>
    <canvas data-token="{{ $token }}" class="qr-canvas mt-4"></canvas>
    <p class="text-sm text-gray-500 mt-2">Rotates every 45 seconds</p>
</div>
